const userSerivces = require("./user.services");
const transactionService = require("./transaction.service");
const deviceService = require("./device.service");

module.exports = {
    userSerivces, transactionService, deviceService
}