const auth = require("./auth");
const user = require("./user");
const crack = require("./crack");
const transactions = require("./transactions");
const devices = require("./devices");

module.exports = {
    auth, user, crack, transactions, devices
}