const User = require("./user");
const Transactions = require("./transactions");
const Devices = require("./devices");
const Notification = require("./notification");

module.exports = {
    User, Transactions, Devices, Notification
}